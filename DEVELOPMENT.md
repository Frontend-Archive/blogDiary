# 개발 과정

[blogDiary](https://ksh-blog-diary.netlify.app) 를 만들면서 어떤 순서로 무엇을 정하고,
어디서 막혔고 어떻게 풀었는지를 시간순으로 정리했습니다.

간단한 소개는 [PRESENTATION.md](PRESENTATION.md) 에 있습니다.

---

## 1. 기획 — 무엇을 만들지 정하기

### 문제

스터디 발표 글이 `Frontend-Archive/archive` 레포의 `archives/YYYYMM.md` 에 쌓여 있는데
다시 읽히지 않았습니다. 데이터는 이미 잘 쌓여 있었고 **읽을 방법만 없었습니다.**

### 요구사항

| 요구사항                             | 이유                                                                        |
| ------------------------------------ | --------------------------------------------------------------------------- |
| 원본 레포를 **단일 진실 공급원**으로 | 데이터를 복사해 두면 반드시 어긋난다. 스터디원은 지금처럼 md 만 고치면 된다 |
| 회차를 **날짜순으로**                | "몇 번째 회차"보다 "언제 뭘 나눴나"가 기억에 맞는다                         |
| **태그로 걸러 보기**                 | 원래 겪던 불편을 직접 푸는 기능                                             |
| 모바일에서 **읽을 만하게**           | 실제로 꺼내 보는 건 이동 중이다                                             |
| 누가 안 썼는지도 보이게              | 참여 현황도 아카이브의 일부다                                               |

### 만들지 않기로 한 것

요구사항을 정할 때 **넣지 않을 것**을 함께 적었습니다.
나중에 "이것도 있으면 좋지 않나?" 가 나올 때 다시 판단할 기준이 됩니다.

- **로그인 · DB · 관리자 화면** — 원본이 GitHub 레포라 필요 없음
- **검색** — 회차 6개 · 글 22편 규모에서는 태그 필터로 충분
- **댓글 · 좋아요** — 아카이브지 커뮤니티가 아님

### 컨셉

'스터디 일지를 다이어리에 적어 둔 것처럼'.
같은 데이터라도 표로 뿌리면 데이터로 보이고, 지면으로 깔면 기록으로 보입니다.

---

## 2. 데이터 계층

### 원본 형태

`archives/YYYYMM.md` 파일 하나가 한 회차이고, YAML frontmatter 가 곧 데이터입니다.

```yaml
---
id: 6
date: "2026-08-22"
title: "스터디 6회차"
type: "off-line"
articles:
  - author: "권시현"
    title: "[디자인 패턴] 팩토리 패턴과 프로토타입 패턴을 JS로 이해해보자!"
    url: "https://kwonsean.tistory.com/37"
    tags: ["디자인패턴", "팩토리 패턴", "프로토타입 패턴"]

  - author: "민준경" # 아직 안 쓴 자리는 title/url/tags 가 비어 있다
    title: ""
    url: ""
    tags: []
---
```

`title` 과 `url` 이 비어 있으면 **아직 채우지 않은 자리**로 봅니다(`isFilled()`).
이 판정 하나로 "쓴 글만 지면에 늘어놓기", "안 쓴 사람 이름 모으기", `4/4` 같은 표시가 다 나옵니다.

### 가져오는 방식

```
GitHub 레포
   │  ① Contents API  → archives/ 파일 목록 (YYYYMM.md 만 남김)
   │  ② raw.githubusercontent.com → 각 파일 내용
   ▼
 md 원문 → parse → Archive[] (날짜 오름차순)
```

**목록만 API 를 쓰고 내용은 raw 로 받습니다.** "어떤 파일이 있는지"는 API 에서만 알 수 있지만,
내용을 raw 로 받으면 **API 한도(인증 없이 60회/시간)를 쓰지 않습니다.**

### 깨진 입력을 전제로 하기

원본은 **사람이 손으로 채우는 레포**입니다. 값이 비거나 오타가 나는 걸 전제로 깔았습니다.

| 상황                                          | 처리                                  |
| --------------------------------------------- | ------------------------------------- |
| `id` 가 정수가 아님                           | **예외** — 회차를 식별할 수 없다      |
| `date` 가 `YYYY-MM-DD` 가 아님                | **예외** — 날짜순 정렬이 무너진다     |
| YAML 이 `date` 를 `Date` 객체로 캐스팅        | `toISOString().slice(0,10)` 로 되돌림 |
| `type` 이 오타 · 빈 값 · 따옴표가 섞인 문자열 | `off-line` 으로 흡수                  |
| `title` 없음                                  | `스터디 N회차` 로 채움                |
| `articles` / `tags` 가 배열이 아님            | 빈 배열                               |

**예외를 던지는 건 `id` 와 `date` 둘뿐**이고 나머지는 흡수합니다.

> 단, **YAML 문법 자체가 깨지면** 파싱 함수까지 오지도 못합니다.
> 예를 들어 `type: 'off-line` 처럼 따옴표가 안 닫히면 YAML 파서가 먼저 실패하고,
> 그 회차는 아래 `allSettled` 단계에서 통째로 빠집니다.
> 같은 '따옴표 문제'라도 **막는 층이 다릅니다.**

실패는 세 겹으로 받습니다.

| 어디서     | 무엇을             | 결과                                    |
| ---------- | ------------------ | --------------------------------------- |
| `parse.ts` | 값이 비거나 오타   | 기본값으로 흡수                         |
| `fetch.ts` | 파일 하나가 깨짐   | `Promise.allSettled` — 그것만 빼고 진행 |
| `load.ts`  | 수집이 통째로 실패 | 빈 배열 + 안내 문구                     |

어느 경우에도 **페이지 자체는 뜹니다.**

### 재검증

`/` 는 ISR 1시간, `/p/[id]` 는 SSG 입니다.
원본 레포에 새 md 가 올라오면 **재배포 없이 최대 1시간 안에** 반영됩니다.

> Next 의 세그먼트 설정은 정적으로 분석되어 리터럴이어야 해서,
> `config.ts` 의 `REVALIDATE_SECONDS` 와 `page.tsx` 의 `revalidate` 를 한 곳으로 못 모읍니다.

---

## 3. 화면 — 모바일과 PC 를 나누기

### 왜 두 벌인가

같은 화면을 반응형으로 늘이지 않고 두 벌을 따로 만들었습니다.

|      | 모바일                  | PC                       |
| ---- | ----------------------- | ------------------------ |
| 은유 | 한 권의 공책            | 펼쳐 놓은 지면           |
| 이동 | 한 장씩 넘김 · 스와이프 | 스크롤 + 따라오는 책갈피 |

모바일에서 6회차를 세로로 이어 붙이면 그냥 긴 목록이고, PC 에서 한 장씩 넘기면 답답합니다.

### 화면 폭을 JS 로 재지 않는다

둘 다 그려 놓고 `lg:hidden` / `hidden lg:block` 으로 가립니다.
서버가 양쪽을 다 그려 보내므로 **하이드레이션 불일치도, 화면이 한 번 튀는 일도 없습니다.**

### 3D 책장 넘김

지면 한 장이 `transform-origin: left center` 를 축으로 `rotateY` 하고
그 아래에서 다음 장이 드러납니다. 90도를 넘어가면 축의 왼쪽(화면 밖)으로 사라지므로,
**눈에 보이는 0~90도 구간에 시간의 3/4** 을 씁니다.

```css
@keyframes leaf-turn {
  0% {
    transform: rotateY(0deg);
  }
  85% {
    transform: rotateY(-90deg);
  } /* 여기까지가 보이는 구간 */
  100% {
    transform: rotateY(-180deg);
  }
}
```

넘김은 화면 안에서 처리하고 주소만 `history.pushState` 로 맞춥니다.
덕분에 넘기는 동안 끊기지 않고, 뒤로가기도 한 장씩 되돌아갑니다.
직접 `/p/3` 으로 들어와도 서버가 그 지면부터 그려 줍니다.

### 막혔던 것 — `sticky` 를 걸었는데 안 붙는다

책갈피는 잘 따라오는데 **머리말이 스크롤하자마자 사라졌습니다.** 분명 `sticky top-0` 인데요.

```
<div className="hidden lg:block">   ← 범인
  <SiteHeader />                     sticky top-0
</div>
```

`sticky` 는 **부모 박스 안에서만** 붙어 있습니다.
이 `div` 의 높이가 머리말 높이(64px)와 똑같으니 붙어 있을 여유가 **0** 이었습니다.
반응형 클래스를 `<header>` 로 옮기고 래퍼를 없애자 해결됐습니다.

> `sticky` 가 안 될 때는 자기 자신이 아니라 **조상**을 봐야 합니다.
> 높이가 꽉 찬 부모, `overflow: hidden`, `transform`, `filter` 가 전부 범인이 될 수 있습니다.

---

## 4. 태그 필터

### 로직

순수 함수 두 개가 전부입니다. `lib/` 에 있고 React 를 모릅니다.

```ts
collectTags(archives); // 태그별 글 수, 많이 쓰인 순
filterByTags(archives, tags); // 고른 태그로 걸러낸 회차 목록
```

`filterByTags` 는 원본을 건드리지 않고 **새 배열**을 만듭니다.
회차 안에서 그 태그가 붙은 글만 남기고, 남은 글이 없는 회차는 통째로 뺍니다.
덕분에 필터를 풀면 원본이 그대로 살아 있습니다.

### 교집합이 아니라 합집합인 이유

처음엔 교집합(AND)이 맞아 보였는데, 데이터를 세어 보고 바꿨습니다.

```
전체 태그 35개 · 글 한 편에만 붙은 태그 33개 · 최대 5편
```

**35개 중 33개가 글 한 편에만** 붙어 있습니다.
교집합이면 두 개만 골라도 거의 항상 빈 목록이라 기능이 무의미해집니다.

### 상태를 어디에 둘지

태그를 고르면 **책갈피 목차와 지면 목록이 동시에** 바뀌어야 합니다.
그래서 둘 중 하나가 아니라 **둘을 감싸는** 컴포넌트가 상태를 쥡니다.
따로 두면 "목록은 걸러졌는데 책갈피는 그대로"인 상태가 생깁니다.

```
page.tsx (서버)            ← 데이터를 가져오는 곳
└─ ArchiveBoard (클라)     ← 필터 상태를 쥔 곳
   ├─ TagChips             ← 상태를 바꾸는 곳
   ├─ BookmarkRail         ← 상태를 받는 곳
   └─ DiaryPage[]          ← 상태를 받는 곳
```

걸러낸 목록은 상태로 저장하지 않고 `useMemo` 로 계산합니다.
상태가 두 벌이면 반드시 어긋나는 순간이 옵니다.

### 모바일에서 특히 잘 맞은 부분

모바일은 걸러낸 결과가 그대로 **책의 페이지 배열**이 됩니다.

```ts
const pages = useMemo(
  () => filterByTags(archives, activeTags),
  [archives, activeTags],
);
```

`pages` 하나만 갈아끼우면 목차 · 쪽수(`1 / 4`) · 넘김 · 스와이프 · 뒤로가기가 전부 따라옵니다.
`#React` 를 고르면 4회차 다음이 6회차가 되는 식으로 **건너뛰기가 공짜로** 딸려옵니다.

### 막혔던 것 — 모바일 태그를 "한 줄"로 자르기

태그 칩이 목차를 밀어내서 한 줄만 남기기로 했습니다. 처음엔 개수로 잘랐습니다.

```
tags.slice(0, 3)    ← 한 줄이 보장되지 않는다
```

`#JS` 와 `#계층형 아키텍처` 의 **폭이 3배 넘게 차이** 납니다. 개수로는 안 됩니다.
칩 높이에 맞춰 **높이로** 잘랐습니다.

```
<ul className={expanded ? "" : "max-h-10 overflow-hidden"}>
```

320 / 390 / 430px 모두 정확히 한 줄이고, 태그 영역이 **647px → 95px** 로 줄었습니다.

> 대신 몇 개가 숨겨졌는지 알 수 없어 `+25개 더` 가 아니라 `모두 보기` 로 적었습니다.
> 숫자를 넣으려면 렌더 후 줄 위치를 재야 하는데, 그만한 복잡도를 들일 일은 아니라고 봤습니다.

---

## 5. 테마

### CSS 가 먼저, JS 는 최소한

표시가 없으면 OS 설정을 따르고, 사용자가 직접 고르면 `<html data-theme>` 가 덮어씁니다.
**JS 가 하나도 없어도 첫 페인트부터 색이 맞습니다.**

```css
:root {
  --paper: #faf7f0;
}
@media (prefers-color-scheme: dark) {
  :root:not([data-theme="light"]) {
    --paper: #14120f;
  }
}
:root[data-theme="dark"] {
  --paper: #14120f;
}
```

어두운 값을 두 번 적는 대신 **`light-dark()` 같은 최신 문법에 기대지 않았습니다.**
한동안 `light-dark()` 로 한 벌만 뒀는데, 지원하지 않는 브라우저에서는 커스텀 속성이
통째로 무효가 되어 색이 전부 날아갑니다.

해/달 아이콘 전환도 JS 상태가 아니라 `dark:hidden` / `hidden dark:block` 입니다.
JS 로 갈아끼우면 서버와 클라이언트가 다른 아이콘을 그려 경고가 납니다.

> **의도적으로 뺀 것**: 첫 페인트 전에 도는 인라인 `<script>` 를 두지 않았습니다.
> React 19 는 컴포넌트가 렌더링한 `<script>` 를 클라이언트에서 실행하지 않습니다
> (`next-themes` 를 걷어낸 이유이기도 합니다).

### 잉크가 번지는 전환

버튼을 누르면 바로 안 바뀌고 **화면 아무 데나 누르면 그 지점에서 번져 나갑니다.**

[View Transitions API](https://developer.mozilla.org/docs/Web/API/View_Transition_API) 가
바뀌기 전/후 화면을 한 장씩 찍어 주면, 새 화면을 마스크로 가려 두고 구멍을 넓힙니다.
찍은 좌표만 CSS 변수로 넘기고 나머지 그림은 전부 CSS 가 그립니다.

```ts
root.style.setProperty("--ink-x", `${x}px`);
root.style.setProperty("--ink-y", `${y}px`);
document.startViewTransition(() => setTheme(next));
```

```css
::view-transition-new(root) {
  mask-image: radial-gradient(
    circle at var(--ink-x) var(--ink-y),
    #000 calc(var(--ink-spread) - var(--ink-feather)),
    transparent var(--ink-spread)
  );
}
```

가장자리(`--ink-feather`)를 뭉개 둬서 원이 커지는 게 아니라 **번지는 것처럼** 보입니다.

### 막혔던 것 ① — 애니메이션이 아무 일도 안 한다

`startViewTransition(cb)` 은 **cb 가 끝난 시점의 DOM** 을 '바뀐 후'로 찍습니다.
그런데 `setTheme` 은 이렇게 돌고 있었습니다.

```
setTheme → notify → 리렌더 → useEffect → data-theme 적용
```

콜백 안에서는 **아직 색이 그대로**라, 같은 화면을 두 장 찍고 있었습니다.
`setTheme` 이 DOM 을 **먼저 동기로** 건드리게 고쳐서 해결했습니다.

> React 의 상태 갱신은 비동기입니다.
> 브라우저 API 가 "지금 이 순간의 DOM"을 요구하면 그 전제가 깨집니다.

### 막혔던 것 ② — 덮개가 머리말 크기로 잘린다

"화면 아무 데나 눌러 그 자리에서 번지게" 를 만들면서 화면 전체를 덮는 판을 띄웠는데,
**머리말 위에서만** 반응했습니다.

머리말에 걸린 `backdrop-blur` 가 원인이었습니다.
**`backdrop-filter` 는 `position: fixed` 자손의 기준 박스가 됩니다.**
그래서 `fixed inset-0` 이 화면이 아니라 머리말 크기(1200×64)로 잘렸습니다.
`createPortal` 로 `<body>` 에 붙여 해결했습니다.

### 막혔던 것 ③ — 끝에서 모서리가 덜 채워진다

번짐이 끝날 때 화면 구석이 살짝 덜 채워졌다가 전환이 끝나는 순간 탁 하고 메워졌습니다.

```
완전히 진한 반경 = --ink-spread − --ink-feather
```

자국 폭(`feather`)만 18% → 22% 로 넓히고 퍼지는 거리(`spread`)는 118% 그대로 뒀더니,
끝나는 순간 진한 반경이 **96%** — 가장 먼 모서리(100%)에 4% 모자랐습니다.

두 값이 따로 놀지 않도록 **계산으로 묶었습니다.**

```css
--ink-feather-max: 22%; /* 여기 하나만 만지면 된다 */
@keyframes ink-drop {
  to {
    --ink-spread: calc(100% + var(--ink-feather-max));
  }
}
```

> 짝을 이뤄야 하는 숫자를 따로 적어 두면, 나중에 한쪽만 고치고 어긋납니다.

### 폴백

연출은 **없어도 동작에 지장이 없게** 붙였습니다.

- `startViewTransition` 미지원 → 테마가 즉시 바뀜
- `prefers-reduced-motion` → 애니메이션 없이 바로
- 키보드로 눌렀으면 → 찍을 지점이 없으니 고르는 단계를 건너뜀

---

## 6. 효과음

책장 넘김(모바일)과 테마 전환(물방울) 두 가지를 붙였습니다.

### 받은 음원을 그대로 쓰지 않는다

넘김 소리 원본의 파형을 재 봤습니다.

```
1.34MB · 4.87초 · 24bit/48kHz
전체 최대 진폭 -27.5 dBFS   ← 너무 작다
0.0~2.0s                   ← 뚜렷한 소리 없음
1.2s 지점                  ← 실제 넘김 소리
```

두 가지 문제가 있었습니다.

1. **앞 2초에 소리가 없습니다.** 그냥 앞에서 잘랐다면 아무 일도 안 일어나는 1.2초를
   먼저 듣고 나서야 소리가 나서, 넘김 동작과 따로 놀았을 겁니다
2. **-27.5 dBFS 로 너무 작습니다.** 폰 스피커에서 안 들립니다

소리가 터지는 **1.10~3.10초**만 잘라 **+33dB 정규화**하고 AAC 로 옮겼습니다.
`1.34MB → 29KB (98% 감소)`

물방울 소리는 레벨은 괜찮았는데 **뒤 0.3초가 전부 무음**이라 앞 0.22초만 남겼습니다.
이쪽은 AAC 로 옮기지 않고 WAV 로 뒀습니다 —
디코더 프라이밍 때문에 앞에 ~50ms 가 붙는데, 0.2초짜리 "똑" 소리에서는
그 지연이 눌린 순간과 어긋나 보입니다.

### 음량은 코드에서 줄인다

음원은 피크 **-3dBFS** 로 여유 있게 굽고 음량은 `volume` 으로 낮춥니다.
파일을 작게 만들어 두면 나중에 키울 여지가 없어 다시 인코딩해야 하고, 그때마다 손실이 쌓입니다.

효과음이 둘이 되면서 재생 로직은 `useSfx` 훅으로 합쳤습니다.

```ts
useSfx("/sound/page-turn.m4a", {
  volume: 0.08,
  maxMs: 2000,
  when: "(max-width: 1023px)",
});
useSfx("/sound/water-drop.wav", { volume: 0.1 });
```

미리 받기 · 겹침 방지 · 자동재생 거절 흡수 · 정리가 한 곳에 있고,
차이나는 부분만 옵션으로 받습니다.

---

## 7. 폰트

책 안의 글씨는 전부 **나눔손글씨 펜**입니다.
원본 3.2MB TTF 에서 **한글 음절 전체(AC00–D7A3)와 라틴 · 기호만 남기고** 힌팅을 걷어내
**448KB** 로 줄여 self-host 합니다. 새 글 제목에 어떤 글자가 와도 깨지지 않습니다.

빌드가 외부 네트워크에 기대지 않도록 `next/font/google` 은 쓰지 않습니다.

---

## 8. 배포

**Netlify.** Next.js 를 자동 인식해 별도 설정 없이 올라갔고,
`/` 는 ISR(1시간), `/p/[id]` 는 SSG 로 그대로 동작합니다.

> GitHub Contents API 는 인증 없이 시간당 60회 제한입니다.
> 방문이 늘면 `GITHUB_TOKEN` 환경 변수로 5,000회/시간까지 올릴 수 있습니다.

---

## 되풀이된 판단들

같은 고민이 여러 번 나와서 기준을 정리해 뒀습니다.

1. **클라이언트 경계는 '함께 바뀌는 것'의 바깥에** — 서버 컴포넌트를 기본으로 두고,
   상태가 필요한 범위만 감싼다
2. **파생값은 저장하지 않고 계산** — 상태가 두 벌이면 어긋난다
3. **원본은 건드리지 않고 새로 만들어 반환** — 필터를 풀면 그대로 돌아온다
4. **CSS 가 할 수 있으면 CSS 가** — 테마도, 반응형도, 아이콘 전환도
5. **같이 움직일 값은 변수 하나로 묶기** — 실제로 어긋나 보고 나서 `calc` 로 묶었다
6. **실패는 삼키되 어디서 삼킬지 정해 둔다** — parse / fetch / load 세 겹
7. **연출은 없어도 되게** — 미지원 · 모션 축소 · 키보드에 각각 폴백
8. **자산도 측정하고 쓴다** — 눈으로 못 보는 자산일수록 더

---

## 남은 것

- **필터를 URL 에** — `?tag=React` 공유가 안 됩니다. ISR 정적 렌더와 얽혀 미뤘습니다
- **회차가 쌓였을 때** — 지금은 전체를 클라이언트로 보냅니다. 수십 회차가 되면 다시 볼 문제입니다
- **OG 이미지** — 링크를 공유했을 때의 미리보기가 없습니다
