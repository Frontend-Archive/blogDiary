# blogDiary

> 스터디에서 발표한 글들을 **다이어리처럼 모아 보는 사이트**

**https://ksh-blog-diary.netlify.app**

개발 과정을 자세히 정리한 문서는 [DEVELOPMENT.md](DEVELOPMENT.md) 에 있습니다.

---

## 1. 서비스 요구사항 정의

### 어떤 문제가 있었나

스터디 발표 글을 `Frontend-Archive/archive` 레포에 회차별로 쌓아 왔습니다.
기록은 잘 되고 있었는데, **다시 읽히지가 않았습니다.**

- 지난 회차를 보려면 md 파일을 하나씩 열어야 한다
- "그 주제 글 누가 썼더라" 를 찾을 방법이 없다
- 모바일에서는 링크 누르기도 불편하다

**데이터는 이미 있었고, 읽을 방법만 없었습니다.**

### 만들 것

| 요구사항                      | 왜                                             |
| ----------------------------- | ---------------------------------------------- |
| 원본 레포를 **그대로** 읽는다 | 데이터를 복사해 두면 반드시 어긋난다           |
| 회차를 **날짜순으로** 훑는다  | "몇 회차"보다 "언제 뭘 나눴나"가 기억에 맞는다 |
| **태그로 걸러 본다**          | 원래 겪던 불편을 직접 푸는 기능                |
| 모바일에서 **읽을 만하게**    | 실제로 꺼내 보는 건 이동 중이다                |
| 누가 안 썼는지도 보인다       | 참여 현황도 아카이브의 일부다                  |

### 안 만들 것

넣지 않을 것도 같이 정했습니다. 나중에 "이것도 있으면 좋지 않나?" 가 나올 때
다시 판단할 기준이 됩니다.

- **로그인 · DB · 관리자 화면** — 원본이 GitHub 레포라 필요 없다
- **검색** — 회차 6개 · 글 22편 규모에서는 태그로 충분하다
- **댓글 · 좋아요** — 아카이브지 커뮤니티가 아니다

---

## 2. 설계

### 데이터를 어떻게 가져올까

**원본 레포를 단일 진실 공급원으로 둡니다.** 따로 복사해 두지 않습니다.

```
GitHub 레포 (회차별 md)
      ↓  읽어서 해석
  회차 목록
      ↓
  페이지 (1시간마다 갱신)
      ├─ 모바일 → 책
      └─ PC    → 지면
```

스터디원은 지금까지처럼 **md 만 고치면 되고**, 새 글이 올라오면 **1시간 안에**
사이트에 알아서 반영됩니다. 다시 배포할 필요가 없습니다.

### 깨진 데이터를 전제로 한다

원본은 **사람이 손으로 채우는 레포**입니다. 값이 비거나 오타가 나는 걸 당연하게 봤습니다.

- 값이 비거나 오타가 나면 → **기본값으로 흡수**
- 파일 하나가 문법까지 깨졌으면 → **그것만 빼고** 나머지는 보여줌
- 아예 못 가져오면 → **안내 문구**와 함께 페이지는 뜸

어느 경우에도 화면이 안 깨지게 했습니다.

### 모바일과 PC 를 따로 만든다

같은 화면을 늘였다 줄였다 하지 않고, **두 가지를 따로** 만들었습니다.

|         | 모바일                    | PC                       |
| ------- | ------------------------- | ------------------------ |
| 모습    | **한 권의 공책**          | **펼쳐 놓은 지면**       |
| 보는 법 | 한 장씩 넘기기 · 스와이프 | 스크롤 (책갈피가 따라옴) |

모바일에서 6회차를 세로로 길게 이어 붙이면 그냥 긴 목록이고,
PC 에서 한 장씩 넘기게 하면 답답합니다. 읽는 방식이 다르다고 봤습니다.

### 컨셉

'스터디 일지를 다이어리에 적어 둔 것처럼'.
같은 데이터라도 **표로 뿌리면 데이터로 보이고, 지면으로 깔면 기록으로 보입니다.**

---

## 3. 구현

### 무엇을 할 수 있나

- 회차를 **날짜순으로** 훑어본다
- **태그로 걸러서** 본다 — 여러 개 골라도 된다
- 글 제목을 누르면 **원문으로** 간다
- 누가 아직 안 썼는지도 보인다

현재 **6회차 · 글 22편 · 태그 35개** 가 올라와 있습니다.

### 태그 필터

칩이 **많이 쓰인 순서로** 깔리고, 누르면 그 태그의 글만 남습니다.
필터를 걸면 **책갈피 목차도 같이** 걸러집니다.

여러 개 고르면 **합쳐서** 보여줍니다. 처음엔 "둘 다 붙은 글"로 만들려다가
데이터를 세어 보고 바꿨습니다 — **태그 35개 중 33개가 글 한 편에만** 붙어 있어서,
그렇게 하면 두 개만 골라도 거의 항상 빈 목록이 됩니다.

### 재미 요소

- **3D 책장 넘김** — 종이가 실제로 돌아가듯 넘어갑니다 (모바일)
- **책장 넘기는 소리** — 넘길 때마다 납니다
- **잉크가 번지는 테마 전환** — 버튼을 누른 뒤 **화면 아무 데나 찍으면**
  그 자리에서 색이 번져 나갑니다. 고르는 동안 커서는 펜촉으로 바뀝니다

연출은 **없어도 되게** 만들었습니다.
지원하지 않는 브라우저나, 사용자가 애니메이션을 줄이는 설정을 켜 두면
그냥 바로 바뀝니다.

### 스택

Next.js 16 (App Router) / React 19 / TypeScript / Tailwind CSS v4
외부 라이브러리는 md 를 읽는 `gray-matter` 하나만 씁니다.

배포는 Netlify 입니다.

---

## 사이드 프로젝트 URL

|             |                                               |
| ----------- | --------------------------------------------- |
| 서비스      | https://ksh-blog-diary.netlify.app            |
| 소스        | https://github.com/Frontend-Archive/blogDiary |
| 원본 데이터 | https://github.com/Frontend-Archive/archive   |
